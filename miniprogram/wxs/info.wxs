  // js代码
  var message = 'Hello World'
  var name = 'coderwhy'

  function sum(num1, num2) {
    return num1 + num2
  }

  function test() {
    return 'test function'
  }

  // 使用commonjs的模块化导出的方法
  module.exports = {
    message: message,
    name: name,
    sum: sum,
    test: test
  }