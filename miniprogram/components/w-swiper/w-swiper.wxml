<swiper class="swiper"
        circular
        autoplay
        indicator-dots
        indicator-active-color="#ff5777">
  <block wx:for="{{banners}}" wx:key="{{item.acm}}">
    <swiper-item>
      <image src="{{item.image}}" mode="widthFix"/>
    </swiper-item>
  </block>
</swiper>